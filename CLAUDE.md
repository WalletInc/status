# status

Public status / uptime page (Upptime + GitHub Actions). Part of the Wallet Inc. workspace; shared context loads via the global memory file
(workspace root `context/` + `CLAUDE.md`), so it is not repeated here.

## Key facts

- Stack: Upptime (GitHub Actions)
- Run / test: config-driven; no local run
- Entry points / gotchas: (fill in as discovered)

## Current focus

- (Rolling handoff: what is in progress, recent decisions, and the next step. Read this at the
  start of a session; update it at meaningful stopping points. Keep it short.)
